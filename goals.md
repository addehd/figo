# Goals
Create perfect html and css from figma object:
Crea

## TODO
- [ ] SVG export functionality
- [ ] if element is close to center by x
        left: 50%;
        transform: translateX(-50%);


fetch comments using the REST API:

Plugin sends file key + node IDs to UI
UI calls the REST API to get comments
Filter by node ID and include in JSON

Figma personal access token. add localStorage
so you only enter the token once.
